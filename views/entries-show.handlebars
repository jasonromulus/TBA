{{!-- <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <h1>{{entry.name}}</h1>
        <p><a href="/entries/{{entry._id}}/edit"> Edit </a></p>
        <p>
            <form method="POST" action="/entries/{{entry._id}}?_method=DELETE">
                <button type="submit"> Delete </button>
            </form>
        </p>

        <!-- Show Entries -->
        {{#each entries}}
            {{> entries}}
        {{/each}}
        
    </div>
</div> --}}