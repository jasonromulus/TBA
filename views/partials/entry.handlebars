    {{!-- <div class="card-block">
        <h4 class="card-text"> {{user.name}}</h4>
        <p class="card-text"> {{moment this.date "dddd, MMMM D YYYY"}}</p>
        <p class="card-text"> {{user.entry}}</p>
        <p>
            <form method="POST" action="/entries{{user._id}}?_method=DELETE">
                <button class="btn btn-link" type="submit"> Delete </button>
            </form>
        </p>
    </div>
</div> --}}